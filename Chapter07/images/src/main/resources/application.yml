eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  http:
    multipart:
      max-file-size: 10MB
  cloud:
    stream:
      bindings:
        input:
          destination: learning-spring-boot1
        output:
          destination: learning-spring-boot1
          group: comments
          content-type: application/json
  application:
    name: images